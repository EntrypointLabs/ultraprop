[features]
resolution = true
skip-lint = false

[programs.localnet]
# programs added in Phase 4

[provider]
cluster = "localnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "echo 'Phase 4 owns Anchor tests'"
